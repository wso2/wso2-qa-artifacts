<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://ws.apache.org/ns/synapse">
    <registry provider="org.wso2.carbon.mediation.registry.WSO2Registry">
        <parameter name="cachableDuration">15000</parameter>
    </registry>
    <taskManager provider="org.wso2.carbon.mediation.ntask.NTaskTaskManager"/>
    <proxy name="jsonProxy" startOnLoad="true" trace="disable" transports="http https">
        <description/>
        <target inSequence="jsonSwitchSeq"/>
    </proxy>
    <sequence name="jsonSwitchSeq">
        <log level="full"/>
        <switch source="json-eval(getQuote.request.symbol)"
            xmlns:ns="http://org.apache.synapse/xsd"
            xmlns:ser="http://services.samples" xmlns:xsd="http://services.samples/xsd">
            <case regex="DELL">
                <log level="full">
                    <property name="DELL" value="=========Symbol is DELL======"/>
                </log>
            </case>
            <case regex="WSO2">
                <log level="full">
                    <property name="WSO2" value="=========Symbol is WSO2======"/>
                </log>
            </case>
            <case regex="IBM">
                <log level="full">
                    <property name="IBM" value="=========Symbol is IBM======"/>
                </log>
            </case>
        </switch>
        <drop/>
    </sequence>
    <sequence name="fault">
        <!-- Log the message at the full log level with the ERROR_MESSAGE and the ERROR_CODE-->
        <log level="full">
            <property name="MESSAGE" value="Executing default 'fault' sequence"/>
            <property expression="get-property('ERROR_CODE')" name="ERROR_CODE"/>
            <property expression="get-property('ERROR_MESSAGE')" name="ERROR_MESSAGE"/>
        </log>
        <!-- Drops the messages by default if there is a fault -->
        <drop/>
    </sequence>
    <sequence name="main">
        <in>
            <!-- Log all messages passing through -->
            <log level="full"/>
            <!-- ensure that the default configuration only sends if it is one of samples -->
            <!-- Otherwise Synapse would be an open proxy by default (BAD!)               -->
            <filter regex="http://localhost:9000.*" source="get-property('To')">
                <!-- Send the messages where they have been sent (i.e. implicit "To" EPR) -->
                <send/>
            </filter>
        </in>
        <out>
            <send/>
        </out>
        <description>The main sequence for the message mediation</description>
    </sequence>
    <!-- You can add any flat sequences, endpoints, etc.. to this synapse.xml file if you do
    *not* want to keep the artifacts in several files -->
</definitions>
