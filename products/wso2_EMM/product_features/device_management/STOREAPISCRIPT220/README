The script invokes and tests the EMM store API. Execute the script with IP/Host and HTTPS port value as parameters. If there are any assertion failures they will be logged to errorlog.log.


e.g.

sh AndroidStoreAPI.sh localhost 9443
